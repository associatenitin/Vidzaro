# Video Deblur service - AI-based video clarity enhancement
# Python 3.10+

# Real-ESRGAN for AI-based video/image enhancement (easier to install than RealBasicVSR)
realesrgan>=0.3.0
basicsr>=1.4.2

# OpenCV for video processing
opencv-python-headless>=4.8.0
numpy>=1.23.0

# PyTorch with CUDA support (falls back to CPU if CUDA unavailable)
torch>=2.0.0
torchvision>=0.15.0

# Pillow for image processing
Pillow>=9.5.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.10
pydantic>=2.0.0
